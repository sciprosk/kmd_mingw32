# Windows kernel-mode driver linked with GNU tools
A small demo to document compiler and linker options for linking a simple Windows kernel-mode driver (WDM) with GNU tools.
Intended to be used with MSYS2 UCRT64.
